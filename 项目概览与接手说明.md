# SimMC 鸢尾花商城利润筛选器 — 项目概览与接手说明

> 版本：`v1.5.0`（manifest / 两处 README 一致）
> 勘察时间：2026-09-22
> 勘察方式：解压全部交接文件 → 通读源码 → `node --check` 语法校验 → 运行单元测试 → 实测线上公开接口
> 结论：**功能完整、可正常运行、无外部依赖、无构建步骤**；主要风险集中在「零自动化测试覆盖 content.js」与「chrome.storage 配额」，详见第 9 节。

---

## 1. 项目定位

一个 **Chrome / Edge Manifest V3 内容脚本扩展**，注入到 SimMC（Minecraft 服务器）的鸢尾花商城页面 `https://mall.vesego.xyz/`，在页面右下角提供「利润排行」按钮，打开一个自定义面板，做三件事：

1. **算利润** —— 比较「最高收购价」与「最低出售价」，只显示正利润商品；支持单件利润与「按报价数量配对后的最大可执行总利润」两种口径。
2. **洗异常价** —— 按同商品买卖报价的中位数倍数，剔除离群报价。
3. **屏蔽港口** —— 把指定港口的买卖报价从计算中整体摘除，黑名单可导入导出。

**不是服务端应用**，不抓 DOM，全部数据来自商城公开 JSON 接口；没有后台脚本（no background / service worker），没有 popup，没有 options 页面。

---

## 2. 目录结构与文件清单

```
simmc-profit-extension/            ← 仓库根（即本次交付的 zip）
├── mall-profit-extension/         ← 扩展源码（加载到 Chrome 的就是这个目录）
│   ├── manifest.json              602 B    MV3 清单
│   ├── profit-core.js          10.5 KB    纯算法层（可脱离浏览器测试）
│   ├── content.js              47.2 KB    编排 / IO / UI / 缓存（单文件，1300+ 行）
│   ├── content.css             14.8 KB    注入样式（全部 .mpe-* 前缀）
│   ├── tests/profit-core.test.js 3.2 KB   Node 断言脚本（非框架）
│   ├── preview.png              208 KB    商店截图素材，源码不引用
│   └── README.md                3.8 KB    使用说明书（面向玩家）
├── mall-profit-extension.zip    224 KB    分发用打包（内含 mall-profit-extension/ 前缀）
├── AGENTS.md                    2.8 KB    ★ AI 代理行为约束（事实上的开发规范）
├── PROJECT_HANDOFF.md           5.2 KB    ★ 需求 / 架构 / 缓存 / 回归基线
├── README.md                    1.1 KB    仓库入口
├── CLAUDE.md / GEMINI.md        各 241 B  仅指向 AGENTS.md 的转发文件
└── .gitignore                    64 B    忽略 .tmp-vesego/、*.crx、*.pem、*.log
```

**交接体系是刻意设计的**：`AGENTS.md` 是唯一权威规范，`CLAUDE.md` / `GEMINI.md` 只是转发壳。`PROJECT_HANDOFF.md` 第 154–162 行明确要求：换 AI 工具后先读这两份文件再动手，且**不要依赖绝对工作区路径**。

---

## 3. 技术栈、外部依赖与运行方式

### 3.1 技术栈

| 项目 | 取值 | 说明 |
|---|---|---|
| 运行宿主 | Chrome / Edge，Manifest V3 | 仅 `content_scripts`，无 background |
| 语言 | 原生 JavaScript（ES2020+，无 TS、无 JSX） | IIFE + `'use strict'` |
| 构建 | **无** | 源码即产物，直接加载 |
| 依赖 | **零 npm 依赖** | 无 `package.json`、无 lockfile、无 node_modules |
| 持久化 | `chrome.storage.local` | 唯一申请权限：`"storage"` |
| 网络权限 | `host_permissions: https://mall.vesego.xyz/*` | 同源请求，无 CORS 问题 |
| 样式 | 手写 CSS + CSS 变量（`--mpe-*`） | 全部类名前缀 `.mpe-`，避让宿主页面 |

`manifest.json` 中 `profit-core.js` 必须先于 `content.js` 注入 —— 前者把 API 挂到 `globalThis.MallProfitCore`，后者第 4–5 行读取，若缺失则静默 `return`。

### 3.2 外部接口依赖（实测确认，2026-09-22）

Base：`https://mall.vesego.xyz/api/mall`

| 端点 | 实测结果 |
|---|---|
| `GET /items?includeTagged=false&limit=100&offset=N` | 200，`total = 8230`，单页耗时约 5.3 s，`limit` 上限 100 |
| `GET /items/{encodeURIComponent(名称)}/offers?includeTagged=false&limit=100&mode=buy\|sell&offset=N` | 200，`石头` buy total=56 / sell total=76，耗时 1.6–3.2 s |
| `GET /ports?includeTagged=false` | 200，**359 个港口**，`{portName, itemCount}` |

实测商品对象字段：
`itemName, minSellPrice, maxBuyPrice, sellAmount, sellOfferCount, buyOfferCount, totalOfferCount, updatedAt, vanillaId, vanillaSort, groupHash, nearbyPorts`

实测报价对象字段：
`id, price, amount, region, nearestPort{distance,name,x,y,z}, owner, tradeType, tagCounts{expired,internal}, world, x, y, z, createdAt, updatedAt`

> ⚠️ 这是**第三方非官方接口**：无版本号、无文档、无 SLA。任何字段改名或鉴权变更都会直接击穿扩展。`nearbyPorts`、`groupHash`、`vanillaSort` 目前未被使用。

### 3.3 运行方式

**玩家安装（开发态）**
1. 打开 `chrome://extensions/` → 开启「开发者模式」
2. 「加载已解压的扩展程序」→ 选择 `mall-profit-extension/` 目录
3. 打开或刷新 `https://mall.vesego.xyz/`

**开发者验证（仓库根执行，PowerShell）**

```powershell
node --check .\mall-profit-extension\profit-core.js
node --check .\mall-profit-extension\content.js
node .\mall-profit-extension\tests\profit-core.test.js   # 期望输出：profit-core tests passed
```

> 本次勘察三条命令全部通过。
> ⚠️ 测试脚本用 `require('../profit-core.js')` 读取 `globalThis.MallProfitCore`，必须在 `mall-profit-extension/tests/` 相对位置运行，否则路径失效。

---

## 4. 架构分层

三层结构，边界清晰，这是这个项目最好的一点：

```
┌─ 表现 / 样式层 ───────────────────────────────────────┐
│  content.css        全部 .mpe-* 前缀，CSS 变量在 #mpe-root 上定义  │
└──────────────────────────────────────────────────────┘
┌─ 编排 / IO 层 ────────────────────────────────────────┐
│  content.js         DOM 注入、接口分页、并发池、缓存、状态机、       │
│                     黑名单导入导出、进度与取消                      │
└──────────────────────────────────────────────────────┘
┌─ 纯算法层（无副作用、可单测）──────────────────────────┐
│  profit-core.js     归一化、正利润行、港口过滤、离群过滤、           │
│                     买卖配对总利润、排序                            │
└──────────────────────────────────────────────────────┘
```

**设计红线（AGENTS.md 第 56–63 行「Change Discipline」）**：所有计算必须确定性，纯算法只能放 `profit-core.js`；算法改动必须补 Node 测试；非必要不加 npm 依赖；用户可见行为变更必须同步 `manifest.json` 版本号与 README。

---

## 5. 关键代码路径索引

### 5.1 `profit-core.js`（约 285 行，导出 `MallProfitCore`）

| 行号 | 函数 | 职责 |
|---|---|---|
| 14–29 | `normalizeItem` | 商品字段归一化，`itemName` 为空则丢弃 |
| 31–52 | `buildUnitRows` | ★ 产出正利润行：`unitProfit = maxBuyPrice - minSellPrice > 0`，同时算 `profitRate` |
| 54–63 | `normalizeOffers` | 报价归一化，港口取 `region` 优先、回退 `nearestPort.name` |
| 65–81 | `filterBlacklistedOffers` | 港口黑名单**精确匹配**，返回 `{offers, blocked}` |
| 83–90 | `median` | 中位数（偶数取中间两值均值） |
| 92–154 | `filterOutlierOffers` | ★ 离群过滤。样本 ≤4 走「最大相邻价差」分支，否则走 `[中位数/N, 中位数×N]` 区间 |
| 160–194 | `buildOfferAnalysis` | ★ 总装：港口过滤 → 离群过滤 → 价格摘要 → 总利润匹配，输出一个扁平分析对象 |
| 195–237 | `calculateMatchedProfit` | ★ 贪心配对：卖价升序 × 买价降序，`buy.price > sell.price` 时按 `min(剩余量)` 撮合 |
| 239–248 | `withTotalProfit` | 把总利润结果合入行 |
| 250–272 | `compareNullable` / `sortRows` | 稳定排序，null 值恒排末尾，`itemName` 用 `zh-CN` 排序做兜底键 |

### 5.2 `content.js`（约 1310 行）

| 行号 | 模块 | 职责 |
|---|---|---|
| 7–15 | `CONFIG` | `pageSize=100`、`listConcurrency=8`、`offerConcurrency=4`、`cacheTtlMs=5min`、`fetchRetries=2`、`maxOffers=20000` |
| 17–22 | `STORAGE_KEYS` | 四个 storage 键，全部带 `.v1` 后缀（已预留版本迁移位） |
| 48–75 | `state` | 单一状态树（商品、缓存、黑名单、模式、筛选、加载态、AbortController） |
| 85–297 | `createUI` | 一次性构建面板 + 港口管理弹窗全部 DOM（模板字符串 + 42 个 ID 注册） |
| 299–366 | `bindEvents` | 事件绑定；Esc 优先关港口弹窗，其次关面板 |
| 368–403 | `openPanel` / `closePanel` / `openPortManager` / `closePortManager` | ★ `openPanel` **只在 `items` 为空时**触发 `loadMarket` |
| 405–432 | `renderPortManager` | 渲染港口勾选列表（搜索 + 计数） |
| 434–451 | `loadAvailablePorts` | 拉 `/ports`（359 项），支持强制刷新 |
| 461–499 | `exportPortBlacklist` / `importPortBlacklist` | 导出带 `format/version/exportedAt` 的 JSON；导入兼容 JSON（对象或数组）/ TXT / CSV |
| 501–513 | `savePortBlacklist` | 变更时清空分析缓存 + 停算 + 落库；未变更则跳过 |
| 514–550 | `setMode` / `renderMode` / `renderSortOptions` | 单件 / 总利润切换，排序项随模式切换 |
| 573–602 | `apiGet` | 带重试（仅 429 / 5xx / 网络错误重试，退避 350ms×(n+1)）、`credentials: include`、`cache: no-store` |
| 604–619 | `mapLimit` | 并发池（用于分页与报价抓取） |
| 621–661 | `fetchMarketItems` | ★ 首屏拿 `total` → 并发补全剩余页 → 按 `itemName` 去重 |
| 663–702 | `loadMarket` | 商品列表装载；`force` 时先删缓存；成功后清空 total / analysis 缓存 |
| 704–711 | `handleRefresh` | 「刷新数据」：清 total/analysis + 清 storage + 强制重载 |
| 713–737 | `fetchAllOffers` | ★ 单商品报价分页（100/页），上限 20000 条 / 250 页 |
| 739–746 | `isFresh` | TTL 判定（5 分钟） |
| 748–778 | `normalizePortNames` / `hashText`(FNV-1a) / `getAnalysisCacheKey` / `hasFreshAnalysis` | 缓存键 = `multiplier:黑名单哈希` |
| 780–865 | `ensureAnomalyAnalysis` | ★ 异常过滤 + 港口屏蔽主流程，每 10 条落盘一次 |
| 866–944 | `ensureTotalProfits` | 总利润主流程（仅当异常过滤关闭且黑名单为空时走此路） |
| 946–981 | `cancelOfferCalculation` / `updateProgress` / `hideProgress` | 停止计算与进度条 |
| 983–1030 | `calculationNeeded` / `runCalculation` / `setBusy` | ★ **手动计算唯一入口**，按钮文案在「开始计算 / 重新计算 / 无需计算」间切换 |
| 1032–1068 | `getVisibleRows` | ★ 渲染前的核心：合并分析结果 → 搜索/数量/原版筛选 → 排序 |
| 1097–1198 | `render` / `renderStatus` / `renderSummary` / `renderTable` | 全量重绘 |
| 1200–1237 | `loadStoredData` | 启动时从 storage 恢复（**过期条目直接丢弃，不触发计算**） |
| 1239–1298 | `persist*` / `clear*` | 落盘与清理 |
| 1300–1310 | `start` | `createUI` → `loadStoredData` → `render` |

---

## 6. 计算口径与业务规则（需求的核心）

### 6.1 单件利润

```
unitProfit = maxBuyPrice - minSellPrice     只保留 > 0
profitRate = unitProfit / minSellPrice × 100
```

### 6.2 总利润（不是「单件利润 × 数量」）

1. 卖价升序、买价降序
2. 当且仅当 `buy.price > sell.price` 时按 `min(买剩余, 卖剩余)` 撮合
3. 累加 `(buyPrice - sellPrice) × 撮合量`

**忽略港口距离、传送与运输成本** —— 这是明确的产品决定，不是遗漏。

### 6.3 异常价过滤档位

| 选项 | 倍数 | 说明 |
|---|---|---|
| 关闭过滤 | `0` | 只用商品列表的价格，不算报价 |
| 严格 | `5×` | 中位数 ±5 倍 |
| **推荐（默认）** | `10×` | 中位数 ±10 倍 |
| 宽松 | `20×` | 中位数 ±20 倍 |

- 样本 > 4：剔除 `price < median/N` 或 `price > median×N` 的报价
- 样本 ≤ 4：取最大相邻价差，若倍数 ≥ N，则移除**数量较少的一侧**（同数量时：卖侧移低价、买侧移高价）
- 买、卖报价**各自独立**算中位数
- 若过滤会把报价清空，则**整体回退不过滤**（防御性设计）
- 港口过滤**先于**异常过滤执行（`buildOfferAnalysis` 第 161–164 行）

### 6.4 港口黑名单

- 匹配方式：对 `offer.region` 或 `offer.nearestPort.name` 做**精确（区分大小写的字符串全等）**匹配
- 导出格式：
  ```json
  { "format": "mall-profit-port-blacklist", "version": 1, "exportedAt": "ISO-8601", "ports": ["星月湾"] }
  ```
  格式字符串见 `content.js` 第 464 行。导入时**不校验** `format` 字段，只要 `.ports` 是数组即可。
- 导入兼容：上述 JSON / 纯字符串数组 / TXT 或 CSV 文本（按 `[\r\n,]+` 切分）
- 存于 `chrome.storage.local` 的 `mallProfit.portBlacklist.v1`
- **改动后必须手动点「开始计算」** 才会作用于报价

### 6.5 缓存与存储

| Storage Key | 内容 | TTL |
|---|---|---|
| `mallProfit.market.v1` | `{savedAt, items[]}`（8230 条） | 5 min |
| `mallProfit.totals.v1` | `{savedAt, entries{itemName:{totalProfit,matchedQty,…,updatedAt}}}` | 5 min |
| `mallProfit.analyses.v1` | `{ "倍数:黑名单哈希": {itemName:{…analysis,updatedAt}} }` | 5 min |
| `mallProfit.portBlacklist.v1` | `string[]` | **永久** |

内存分析缓存键 = `${anomalyMultiplier}:${FNV-1a(排序后黑名单)}`。**黑名单一变，键就变，旧缓存天然失配** —— 这是一个很聪明的设计，但也意味着历史键会累积（由 `clearAnalysisStorage()` 整体清理）。

### 6.6 三条不可违反的产品红线（AGENTS.md 第 9–17 行）

1. **只在用户点击「开始计算 / 重新计算」时才发起昂贵的报价抓取。** 打开面板可以读商品列表，但绝不自动算报价。
2. **重新打开面板必须直接展示缓存结果**，不得重算。
3. **切换利润模式 / 异常档位 / 原版筛选 / 港口黑名单后，保持「手动」语义**，不得偷跑计算。

> 代码层面的实现点：`openPanel`（368 行）只调 `loadMarket`；`catch` 分支里 `isFresh` 过期**只丢弃、不触发**；`ensureAnomalyAnalysis`（780 行）/ `ensureTotalProfits`（866 行）只有 `runCalculation`（995 行）以 `force=true` 调用。**这三处是回归测试最该盯的地方。**

### 6.7 UI 结构

- **选择器**：利润模式（单件/总利润）、排序、搜索、最低在售数量、商品版本（全部/仅原版/仅非原版）、异常价格档位、港口黑名单
- **右上操作**：「开始计算 / 重新计算 / 无需计算 / 读取中… / 计算中…」（文案随状态切换）、「刷新数据」、关闭
- **摘要卡**：筛选结果数 / 最高单件利润 / 最高总利润
- **表格**：排名、商品、最低出售价、最高收购价，之后按模式分叉（总利润模式多出「总利润 / 可匹配数量」，单件模式多出「在售数量」）
- **「原版」判定**：`vanillaId` 非空即为原版
- 全部面向用户文案为**中文**（AGENTS.md 强制要求）

---

## 7. 当前完成状态

**v1.5.0 已是一个功能闭环、可直接使用的版本。**

`PROJECT_HANDOFF.md` 第 145–152 行记录的回归基线（本次勘察已复现关键数字）：

| 指标 | 基线值 | 本次复核 |
|---|---|---|
| 商品总数 | 8,230 | ✅ 接口实测 `total = 8230` |
| 原始正利润商品 | 430 | 未逐条复算（需全量抓取，约 1–2 分钟） |
| 10× 过滤后结果 | 354 | — |
| 屏蔽「星月湾」「五月花港」后 | 343 | ✅ 两个港口名均在 `/ports` 与报价中真实出现 |
| 面板开/关不触发重算 | 通过 | ✅ 代码路径已确认 |
| 黑名单 JSON 导入导出 | 通过 | ✅ 代码路径已确认 |

已完成的工程化配置：`node --check` 语法校验通过、单元测试通过（覆盖 8 个算法函数、约 30 条断言）、`.gitignore` 就绪、`CLAUDE.md`/`GEMINI.md` 转发壳就绪、发布 zip 与源码目录**逐字节一致**（已用 MD5 逐一比对 7 个文件，全部 SAME）。

---

## 8. 上手 Checklist

```powershell
# 1. 语法与算法回归
node --check .\mall-profit-extension\profit-core.js
node --check .\mall-profit-extension\content.js
node .\mall-profit-extension\tests\profit-core.test.js

# 2. 接口现状探活（确认第三方字段未变）
curl "https://mall.vesego.xyz/api/mall/items?includeTagged=false&limit=1&offset=0"
curl "https://mall.vesego.xyz/api/mall/ports?includeTagged=false"

# 3. 浏览器手工回归（改算法/缓存后必做）
#    chrome://extensions/ → 加载已解压的 mall-profit-extension/
#    ① 打开面板 → 确认无自动计算
#    ② 点「开始计算」→ 看进度、结果数、排序、缓存
#    ③ 关闭再打开 → 确认直接出缓存、不重算
#    ④ 换异常档位 → 按钮回到「开始计算」
#    ⑤ 港口黑名单改一个 → 确认需手动重算
```

---

## 9. 需要注意的问题（按优先级）

### P0 — 无自动化覆盖的模块最大

**`content.js` 1310 行零测试、零 lint、零 CI。** 而它承载了全部并发池、AbortController 取消语义、缓存键、手动计算门控、storage 读写。回归完全依赖手工浏览器验证。任何改动都可能悄悄破坏第 6.6 节的「三条红线」。
→ **建议优先做**：为一个可 mock 的 `chrome.storage` + `fetch` 建立 Node 测试骨架，至少覆盖 `isFresh` / `getAnalysisCacheKey` / `loadStoredData` 的「过期只丢弃不计算」语义。

### P1 — 具体缺陷

1. **港口勾选后列表滚动位置归零**（`content.js` 349–356 → 405–432）
   checkbox 的 `change` 处理器调用 `renderPortManager()`，整个列表 `innerHTML` 被重写，滚动条复位。实测 359 个港口，滚动到底部勾选时会明显「弹回顶部」。
   → 修法：事件委托 + 只改 `checkbox.checked` 与计数文案，不重写 list。

2. **导入 JSON 边界会静默清空草稿黑名单**（`content.js` 479–499）
   `JSON.parse` 成功但结果既非数组、又无 `.ports` 字段时（例如文件内容就是 `123` 或 `{}`），`ports` 为 `undefined` → `normalizePortNames(undefined)` → `[]` → **静默清空**当前勾选，无任何错误提示。
   → 修法：校验 `ports` 必须为数组，否则抛「文件格式不正确」。

3. **`chrome.storage.local` 配额风险**
   未申请 `unlimitedStorage`。MV3 `chrome.storage.local` 默认约 10 MB。当前 `persistMarket` 存 8230 条商品对象、`persistAnalysisCache` 存 400+ 条分析结果，仍在安全区，但一旦商品数或报价量增长（或黑名单缓存键版本累积），会触发 `QUOTA_BYTES_EXCEEDED`。而**代码只 `console.warn`，用户侧表现为「缓存莫名其妙失效、每次打开都重算」**（因为配额满后读回的是空数据）。
   → 修法：加 `unlimitedStorage` 权限，或只持久化精简字段、给 analyses 设条数上限。

4. **`fetchAllOffers` 静默截断**（`content.js` 713–737）
   双重上限 `offers.length < 20000` 与 `guard < 250`，超出后**静默返回部分报价**，不报错也不提示。极端商品（如「石头」sell 有 76 条，尚安全；但热门商品可能接近上限）会得到偏小的总利润。
   → 修法：触顶时在 `state.offerErrors` 或 UI 上标记「报价过多，已截断」。

5. **浮点累加**（`profit-core.js` 195–237）
   全程 JS `Number`，实测价格存在 `2.5` 这类小数。`totalProfit += revenue - cost` 在数千次迭代后可能出现 1e-12 级误差。撮合循环已用 `1e-9` 容差，展示层 `Intl` 限制 2 位小数，**当前不影响观感**；但若要严格对账（例如跟游戏内实际交易比对），建议改整数分（×100）。

6. **港口精确匹配的实际摩擦**（`profit-core.js` 65–81）
   实测 `/ports` 返回 359 项，其中包含「波鲁瓦中心港-港口要素缺失 请重新申请」这类脏名；且 `region` 与 `nearestPort.name` 并非永远相同（扩展取 `region` 优先）。用户手输港名极易漏配。
   → 建议：`/ports` 列表勾选为主路径（已支持）、手输为辅；后续可考虑加「忽略大小写 / 去空白」的归一化开关。

### P2 — 工程与维护性

7. **版本号需手工同步三处**：`manifest.json` `version`、`mall-profit-extension/README.md` 第 5 行、根 `README.md` 第 5 行。AGENTS.md 要求同步，但无强制手段。
8. **测试脚本无框架**：`tests/profit-core.test.js` 是裸 `node:assert` 串行断言，失败时只报第一处。当前够用，规模上来后建议换 `node:test`。
9. **`render()` 每次按键全量重算**：`renderSummary()` 与 `renderTable()` 各调一次 `getVisibleRows()`，每次对全量行做 map/filter/sort。354–430 行量级无感；若异常过滤后行数上千需加节流。
10. **模态框无焦点陷阱**：港口管理弹窗有 `role="dialog"` + `aria-modal="true"`，但 Tab 仍会跑到页面背后；表格也没有 `<caption>`。
11. **Escape 监听挂在 `document` 上**，面板打开时会 `documentElement` 加 `.mpe-panel-open`（`overflow: hidden`）。若商城是 SPA，未关面板就切换路由可能残留滚动锁。需实测确认。
12. **发布包嵌套陷阱**：`mall-profit-extension.zip` 内含 `mall-profit-extension/` 前缀，在仓库根直接解压会得到 `mall-profit-extension/mall-profit-extension/`。
13. **`.gitignore` 未忽略 `*.zip`**：发布包随仓库走（据根 README 描述属有意为之），因此**必须保证 zip 与源码目录不脱节**。本次已 MD5 校验一致，建议在 CI 或提交前加一步校验。

### P3 — 外部风险（不可控，需监控）

14. **接口是第三方非官方端点**，无版本号、无文档。实测 `/items` 首屏约 5.3 s，`limit` 上限 100。任何字段改名立刻击穿扩展 —— 且扩展**没有做字段存在性校验**，例如 `page.items` 不是数组时会被静默当成空。
    → 建议在 `apiGet` 之后加一层最小 schema 校验，字段缺失时给出明确错误而非「0 个商品」。
15. **`credentials: 'include'`** 会带上会话 Cookie。当前同源、无鉴权可正常调用；一旦站方引入 CSRF token 或登录墙，扩展立即失效。
16. **`apiBase` 硬编码**（`content.js` 第 8 行），无配置项，切换环境需改代码。
17. **`maxOffers=20000` / `guard=250` / `cacheTtl=5min` / 并发数 8 与 4 均为硬编码**，未暴露给用户。若接口限流收紧，需要改代码重新发版。

---

## 10. 交接信息缺口

交接包已相当完整（`AGENTS.md` + `PROJECT_HANDOFF.md` 基本覆盖），但以下内容缺失，接手时需向原作者确认：

- **授权/归属**：`mall.vesego.xyz` 是第三方 Minecraft 服务器商城，扩展未获官方背书。接口使用是否有约定或限制、是否可公开发布到 Chrome Web Store，未见说明。
- **Git 历史**：交付物只有工作树快照，没有 `.git` 目录。无法追溯 v1.5.0 之前的变更动机。
- **`preview.png` 用途**：208 KB 商店截图素材，源码未引用，发布流程未记录。
- **证书/签名**：`.gitignore` 忽略 `*.crx` / `*.pem`，说明曾考虑打包发布，但无 `key` 字段，未做固定扩展 ID。
- **浏览器兼容下限**：代码用了 `String.prototype.replaceAll`、可选链、`??`，实际要求 Chrome 85+，未在文档中声明。

---

## 11. 后续开发建议（优先级排序）

1. **先补测试再改代码** —— 为 `content.js` 的手动计算门控、缓存键、过期丢弃语义建立 Node 测试。这是所有后续改动的地基。
2. **修 P1-1（滚动复位）与 P1-2（导入静默清空）** —— 两个都是小改动、高频触达、用户可感知。
3. **加 `unlimitedStorage` 或瘦身持久化** —— 消除「缓存莫名失效」这类难以复现的疑难问题。
4. **给接口加最小 schema 校验** —— 把「接口悄悄变了」从「静默显示 0 条」变成一条明确的错误提示。
5. **抽出版本号到单一来源** —— 例如从 `manifest.json` 读取并注入 UI（`chrome.runtime.getManifest()`），消除三处手工同步。
6. **把 zip 与源码一致性校验固化成一条脚本** —— 避免发布包与源码脱节。

---

*本文档基于对交付包的完整静态勘察 + 线上接口实测生成，所有行号引用以 v1.5.0 源码为准。*
